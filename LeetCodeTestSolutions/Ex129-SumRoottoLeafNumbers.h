#include "Commons.h"
using namespace std;

namespace LeetCodeTestSolutions
{
    class Ex129
    {
    public:
        int sumNumbers(TreeNode *root);
        int sumNumbers(TreeNode *node, int prefix) ;
    };
}